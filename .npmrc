# Only settings that every npm-compatible client understands live here.
# pnpm-specific project settings are in pnpm-workspace.yaml (the pnpm >= 10 home
# for them) so that running `npm` in this repo does not emit "unknown config".

# Refuse to install on a Node version this project does not support.
engine-strict=true
