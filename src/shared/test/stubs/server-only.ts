/**
 * Test stub for the `server-only` package.
 *
 * The real package throws on import outside a React Server Component, which is
 * exactly what makes it useful in the app and useless in a unit test. Vitest
 * aliases the import here so a server module can still be exercised directly.
 */
export {}
